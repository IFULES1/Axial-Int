# --- Web framework ---
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.33
pydantic>=2.9,<3
pydantic-settings>=2.6,<3

# --- Database ---
sqlalchemy>=2.0.36,<2.1
alembic>=1.14,<2
psycopg[binary]>=3.2,<4

# --- HTTP / clients (versions left to the resolver to avoid httpx conflicts) ---
httpx>=0.27,<0.29

# --- LLM / vector / providers (used from P2 onward) ---
openai>=1.57
anthropic>=0.40
qdrant-client>=1.12,<2
pypdf>=5.1,<6
python-docx>=1.1,<2
openpyxl>=3.1,<4
# OCR des PDF scannés (nécessite les binaires système tesseract-ocr + poppler-utils)
pytesseract>=0.3.13,<0.4
pdf2image>=1.17,<2

# --- Auth (Supabase) ---
supabase>=2.9
pyjwt>=2.9,<3
email-validator>=2.2,<3

# --- Billing / reports (P5) ---
stripe>=11.0,<13
reportlab>=4.2,<5
pillow>=10,<13

# --- Worker / scheduling (P6) ---
apscheduler>=3.10,<4

# --- Veille : RSS parsing (P7) ---
feedparser>=6.0,<7

# --- Utilities ---
python-multipart>=0.0.18
loguru>=0.7,<0.8

# --- Dev / test ---
pytest>=8.3,<9
pytest-asyncio>=0.24,<0.25
ruff>=0.8,<0.9
