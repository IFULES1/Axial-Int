# Snapshot — 2026-08-13 (soir) — APIs, RAG, base de connaissance

## Objectif de la session
Choisir les APIs (recherche/embeddings/LLM), les brancher, constituer la base de connaissance
(RAG) à partir des documents de l'utilisateur, et faire tourner le cœur produit en réel.

## Tâches complétées
- [✓] **Benchmark APIs** → `docs/BENCHMARK_APIS_ET_DECISIONS.md` (prix live inclus). Décisions :
  recherche = **Exa+Tavily+Linkup** (multi + rerank Cohere), embeddings = **Cohere** (1 seul modèle),
  LLM = **Gemini** (chat/brouillon) + **Claude Sonnet** (rapport final). Gemini préféré à Mistral.
- [✓] **Doppler** installé et branché (projet `axial`, config `dev`). Toutes les clés migrées. On lance TOUT via `doppler run -- …`.
  `scripts/check_keys.py` → **7/7 clés valides** (Gemini, Cohere embed+rerank, Tavily, Exa, Linkup, Anthropic).
- [✓] **Couche recherche** `app/shared/search/` (base, providers Exa/Tavily/Linkup, rerank Cohere, orchestrator fan-out→dédup→rerank→top-K). TESTÉ réel : requête FR → 6 sources rerankées.
- [✓] **LLM 2 vitesses** `app/shared/llm_client/` : `gemini.py` + `claude.generate` + `generate(tier)`. Modèle chat = **`gemini-flash-latest`** (les gemini-2.5-* font 404 pour cette clé ; -latest = anti-retrait).
- [✓] **`analysis.run_analysis` rebranché** : search multi-moteurs + RAG interne → prompt → PII guard → `generate(tier)`. TESTÉ RÉEL : « marché fintech B2B France 2025 » → analyse sourcée, chiffrée, citée (France FinTech 2025, +36%…). **LE CŒUR MARCHE.**
- [✓] **`intelligence.post_message` aligné** sur le nouveau moteur (search + Gemini tier chat).
- [✓] **Embeddings → Cohere** (`rag/embeddings.py`, `embed-multilingual-v3`, dim 1024, batchs 96, **retry+backoff sur 429**).
- [✓] **Qdrant persistant sur disque** (`QDRANT_URL=file:/Users/mirad/axial-intelligence/data/qdrant`). Recherche combinée **docs user + base de connaissance** (`rag/service.retrieve`, `vector_store` 2 collections : `documents` / `knowledge_base`).
- [✓] **Tri du corpus** `scripts/triage_corpus.py` : 393 fichiers bruts → **249 gardés** copiés dans
  `data/knowledge_base/` (6 catégories), 13 doublons + 22 hors-sujet ONU/BID + 3 internes Axial écartés, déchets (Opera app) ignorés.
- [✓] **Ingestion KB** `scripts/ingest_knowledge_base.py` (extract→chunk→embed Cohere→collection knowledge_base + métadonnées, **reprenable** via `has_document`).

## Décisions techniques prises
- **Embeddings = B (Cohere payant) maintenant → bascule C (BGE-m3 auto-hébergé) plus tard** (cf. `docs/PISTES_AMELIORATION.md`, RAPPEL prioritaire). Raison B : ~1 $ one-time + débloque en 15 min. Raison C plus tard : 0 €, sans limite, données chez soi (RGPD/souveraineté).
- `gemini-flash-latest` plutôt que gemini-2.5-flash (404 sur cette clé + anti-retrait).
- Qdrant embedded sur disque (mono-process) pour le dev ; **vrai serveur Qdrant en prod** (piste).
- Un seul modèle d'embeddings pour base ET requêtes (contrainte espace vectoriel).

## État courant du système
- **Backend** : lancer via `doppler run -- .venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8090 --reload`.
- **Front** : `cd frontend && doppler run -- npm run dev -- -p 3005` (design = prototype exact, compilé Next).
  Branché : auth, onboarding, crédits (réels), identité, session, logout. **PAS encore branché : le chat Workspace au vrai moteur.**
- **Base de connaissance** : 249 docs classés dans `data/knowledge_base/` (01_macro 91, 02_sectoriel 21, 03_reglementaire 50, 04_marche 35, 05_methodo 4, 06_litterature 48).
  **Indexée dans Qdrant : SEULEMENT 13 docs / 1173 chunks** (le reste a échoué en **429 rate-limit du trial Cohere**).
- Comptes test : `founder2@axialtest.com` / `password123` (onboardé).
- ⚠️ Docker occupe 8080/3000 (ancien insight-map) — ne pas toucher. Axial = 8090 (api) + 3005 (front).

## Problèmes résolus / en suspens
- **429 Cohere à l'ingestion** : clé **trial** trop limitée → ajouté retry+backoff + reprise, MAIS ça rame trop sur trial.
  → **Résolution décidée : passer sur clé Cohere PAYANTE** (option B). Run d'ingestion trial stoppé (futile).
- gemini-2.5-flash 404 → `gemini-flash-latest`.
- Qdrant embedded mono-process : ne pas lancer backend + ingestion en même temps (verrou).

## Prochaines étapes (ordre de priorité) — à la reprise
1. **Passer la clé Cohere en payante** (dashboard Cohere → carte → clé prod) puis
   `doppler secrets set COHERE_API_KEY` avec la nouvelle valeur.
2. **Relancer l'ingestion** (backend arrêté) : `doppler run -- .venv/bin/python scripts/ingest_knowledge_base.py`
   (elle **reprendra** et sautera les 13 déjà faits ; ~10-15 min sans le rate-limit).
3. **Vérifier** le compte final (points KB) + lister les docs échoués (PDF scannés → OCR plus tard).
4. **Brancher le chat Workspace au front** sur le vrai moteur (post_message est déjà aligné) + **test e2e à l'écran** :
   question dans l'UI → analyse sourcée (web + base de connaissance) → réponse citée.
5. (plus tard) hooks capture dataset pour fine-tuning ; durcissement VPS ; Doppler prd.

## Contexte à ne pas oublier
- **Doppler** = source de vérité des secrets. TOUT se lance avec `doppler run -- …`. `.env` renommé `.env.backup-avant-doppler`.
- **Reprise ingestion** = idempotente (uuid5 + `has_document`) → relancer ne double pas.
- `docs/PISTES_AMELIORATION.md` = backlog (bascule B→C embeddings, sécu VPS, prod, fine-tuning, DOCX/OCR, batch/cache).
- Le code LLM/embeddings/recherche est **provider-agnostique** (config) → changer de modèle = 1 variable.
- Snapshots antérieurs : …, `snapshot_2026-08-13_coeur.md` (le cœur marche), celui-ci (APIs+RAG+corpus).
