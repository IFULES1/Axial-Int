# Snapshot — 2026-08-10

## Objectif de la session
Reprendre la main sur l'app Axial Intelligence (ex "insight-map"), la stabiliser en production, puis **démarrer sa reconstruction from scratch** en monolithe modulaire — avec pour objectifs produits : création de comptes simple, algo résilient, suivi utilisateurs (analytics), UX mobile responsive.

## Contexte projet (rappel)
- **Axial Intelligence** = plateforme d'intelligence stratégique. 3 modes : Workspace (chat libre), Agents Stratégiques (Market Scanner/PESTEL + Competitor Radar/Porter), Rapports (PDF). + couche Mémoire (profil entreprise injecté auto) + système de Crédits (5 plans, Stripe).
- **Prod** : VPS Hostinger `72.62.25.156` (srv1195764, Ubuntu 24.04), code dans `/opt/insight-map`, branche `release/prod-20260603`, ~21 conteneurs Docker (11 microservices + Supabase self-hosted + Qdrant + Langfuse).
- **Repo local source** : `/Users/mirad/insight-map` (référence pour le portage).
- **Nouveau monolithe** : `/Users/mirad/axial-intelligence` (en construction).

## Tâches complétées cette session
- [✓] **Accès VPS rétabli** : nouvelle clé SSH `~/.ssh/hostinger_vps` + raccourci `ssh hostinger` (config `~/.ssh/config` → root@72.62.25.156). Clé publique ajoutée via hPanel par l'utilisateur.
- [✓] **Audit sécurité VPS** : pare-feu Hostinger amont OK (Postgres/Qdrant/services internes filtrés depuis internet ; seuls 22/80/443/8000/8080/8443 ouverts). Points chauds : SSH root+password activé + 135 tentatives brute-force/7j + pas de fail2ban ; `.env` en 644 (world-readable) ; `.env.bak` de secrets. **Non encore corrigés** (proposés, pas appliqués).
- [✓] **Bug "API Anthropic ne marche pas" résolu en prod** : le modèle `claude-sonnet-4-20250514` était périmé (EOL 15/06/2026 → 404). Remplacé par **`claude-sonnet-5`** dans `/opt/insight-map/.env` (CLAUDE_MODEL + ajout CLAUDE_ENRICHMENT_MODEL) ET dans `docker-compose.axial.yml` (agent-service). Conteneurs **recréés** (`up -d --force-recreate`, pas `restart` qui ne relit pas le .env). Testé OK sur backend-service + axial-agent-service.
- [✓] **Bug "recherche internet via Claude indisponible" diagnostiqué** : c'est un fallback — la vraie cause est la **clé Perplexity `pplx-C3R...` en quota dépassé (401 insufficient_quota)**. Cette clé traînait en clair dans 10 fichiers .md du repo (fuite). L'utilisateur a dit régler ça de son côté ("c'est bon carrément").
- [✓] **Plan de reconstruction approuvé** : sauvegardé dans `/Users/mirad/.claude/plans/propose-un-plan-pour-snoopy-meteor.md`. Monolithe modulaire + worker + sidecar PII. Décisions clés verrouillées (voir ci-dessous).
- [✓] **P0 (Socle) terminé et vérifié** : scaffold `~/axial-intelligence` — app FastAPI qui démarre, 3 tests de fumée passent, ruff clean.

## Décisions techniques / produit verrouillées
- **Nom du projet** : "Axial Intelligence" (plus "insight-map").
- **Architecture** : reconstruction from scratch en **monolithe modulaire** (1 API FastAPI + 1 worker + 1 sidecar PII), remplace les 11 microservices. L'app prod actuelle tourne jusqu'à la bascule.
- **Auth** : `POST /auth/register {email, password}`. **Conserver** blocage freemail + onboarding forcé. **Supprimer** la double identité (UUID Supabase ↔ user_id entier legacy) → **UUID partout**.
- **Données** : migration conservée (users existants se reconnectent sans reset, mêmes UUID/hash).
- **Analytics** : nouveau projet Supabase de suivi (first_seen_at, last_seen_at, quota, KPI extensibles via table events JSONB, RLS). **HÉBERGEMENT DIFFÉRÉ** (voir blocage ci-dessous).
- **Stack cible** : FastAPI + SQLAlchemy 2.0 + **Alembic** (fini les .sql montés à la main) + pydantic-settings + Next.js. Provider LLM abstrait (`llm_client`) pour dégradation propre.
- **UX** : mobile responsive obligatoire (breakpoints 375/768/desktop).

## État courant — P0 livré
Structure `/Users/mirad/axial-intelligence/` :
- `app/main.py` (FastAPI, CORS liste blanche, monte modules), `app/config.py` (pydantic-settings, fail-fast), `app/db.py` (SQLAlchemy 2.0 + Base), `app/errors.py` (AppError + handler global), `app/shared/health.py` (état réel des providers).
- `app/modules/{auth,analytics,documents,rag,analysis,intelligence,billing,reports,pii}/` — seul `auth/router.py` a un stub. Le reste = dossiers vides avec `__init__.py`.
- `alembic/` (env.py branché sur la config, script.py.mako), `alembic.ini`.
- `requirements.txt` (pins relâchés pour éviter conflit httpx), `Makefile` (install/run/migrate/seed-users/test...), `docker-compose.yml` (postgres+qdrant en localhost only), `.env.example`, `.gitignore`, `README.md`.
- venv : `.venv/` installé, tout importe. `.venv/bin/pytest` → 3 passed. `ruff` → clean.
- Endpoints live : `GET /health`, `GET /health/providers`, `GET /auth/ping`.

## Problèmes résolus
- Conflit deps httpx (supabase/openai/anthropic/qdrant) → pins relâchés en ranges dans requirements.txt.
- Imports inutiles (Field, PostgresDsn) dans config.py → retirés, ruff clean.

## Blocage en cours
- **Supabase analytics non créé** : org "IFULES1's Org" (`ruoxgzvfqdhnuzkvmymg`) plafonnée à **2 projets actifs gratuits, déjà atteints** (FDTour MVP + axial-investisseurs-dilutif actifs ; IFULES1's Project + Axial—Watches—Test en pause). Coût nouveau projet = 0$ mais bloqué par la limite. Options proposées : co-localiser dans un schéma `analytics` (gratuit, recommandé) / passer Pro ~25$/mois / mettre un projet en pause. **Utilisateur a choisi de trancher plus tard** → on livre les migrations + module analytics prêts à appliquer.

## Prochaines étapes (ordre de priorité)
1. **P1 — Auth + Analytics** : implémenter `POST /auth/register {email,password}` (freemail + onboarding conservés, UUID), modèles SQLAlchemy + 1ère migration Alembic, module `analytics` (tables user_profiles/usage_counters/events + RLS) prêt à appliquer, script `make seed-users`.
2. Trancher l'hébergement Supabase analytics (co-localisation recommandée).
3. P2 Documents+RAG → P3 Analysis+llm_client → P4 Intelligence → P5 Billing+Reports → P6 Worker+PII → P7 Migration & bascule.
4. (Sécurité prod, séparé) durcir SSH (PasswordAuthentication no + fair2ban), chmod 600 .env, supprimer .env.bak, révoquer clé Perplexity fuitée.

## Contexte à ne pas oublier
- **Accès prod** : `ssh hostinger`. Recréer les conteneurs après modif .env = `docker compose up -d --force-recreate <svc>` (le `restart` ne relit PAS le .env). agent-service est dans `docker-compose.axial.yml` (réseau `insight-network` → recréer avec `-f docker-compose.yml -f docker-compose.axial.yml`).
- **Modèles Claude actuels valides** (testés avec la clé prod) : `claude-sonnet-5`, `claude-opus-4-8`, `claude-haiku-4-5-20251001`. Choisi sonnet-5 pour l'enrichissement.
- **Supabase org id** : `ruoxgzvfqdhnuzkvmymg`. Outils MCP Supabase dispo (create_project etc.).
- **Réflexe demandé par l'utilisateur** : refaire un `/project-memory-snapshot` régulièrement pour tracer le travail.
- venv Python 3.13 ; lancer les commandes via `.venv/bin/...` depuis `/Users/mirad/axial-intelligence`.
