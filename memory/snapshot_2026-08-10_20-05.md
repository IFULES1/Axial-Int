# Snapshot — 2026-08-10 (Câblage + Couche Mémoire)

## Objectif
1) Câbler crédits + rapports + analytics dans le flux analysis/intelligence.
2) Implémenter la couche Mémoire (profil entreprise + injection auto + gate onboarding).

## Câblage (terminé)
- `analysis/service.py` :
  - `precheck_credits(db, user_id, type, is_admin)` : 402 si pas assez de crédits AVANT l'appel API (admin bypass).
  - `finalize(db, user_id, type, result, is_admin)` : si NON dégradé → consume_credits (atomique) + create_report + analytics.increment_usage(analyses=1, credits, reports=1) ; **dégradé = 0 crédit, pas d'archive**. Écrit report_id + charged dans metadata.
  - `stream_analysis` : précheck upfront (erreur SSE si 402) + finalize après succès. Signature ajoute db, is_admin.
- `analysis/router.py` : /run et /stream passent db + is_admin ; /run fait precheck → run → finalize.
- `intelligence/service.py post_message` : précheck agent_message (2cr), débit après réponse réelle uniquement (dégradé non facturé) + analytics.increment_usage(agent_messages=1). Param is_admin.
- Fix cross-dialecte : JSONB → `JSON().with_variant(JSONB,"postgresql")` (reports.sources, messages.citations) pour tests SQLite.
- Fix tz : billing `_trial_active` normalise en UTC (`_as_aware`) — naive/aware safe (SQLite + prod).

## Couche Mémoire (terminée)
- `app/modules/memory/models.py` : `CompanyProfile` (user_id PK + 11 champs : company_name, positioning, sector, founding_year, funding_stage, team_size, country, target_market, client_segment, known_competitors, main_challenge).
- `service.py` : get/upsert/delete_profile, `onboarding_complete()` (= profil avec company_name non vide), **`build_context()`** (bloc "## Contexte entreprise (mémoire)" injectable, "" si vide). upsert filtre les champs non autorisés (_EDITABLE).
- `router.py` : GET/PUT /memory/profile, GET /memory/onboarding-status, DELETE /memory/profile.
- **Injection auto câblée** : run_analysis prend `company_context` (préfixé au prompt) ; router /run + stream_analysis appellent memory.build_context ; intelligence.post_message injecte company_context + doc_context (remplace le hook TODO(memory)).
- Migration `0004_memory.py` (company_profiles). env.py + main.py à jour.
- Tests `tests/test_memory.py` + `tests/test_wiring.py`.

## État vérifiable
- `.venv/bin/pytest -q` → **34 passed**. ruff clean.
- 7 tables : company_profiles, conversations, credit_balances, documents, messages, projects, reports.
- `alembic heads` → **0004_memory (head)** unique (chaîne 0001→0002→0003→0004).
- **34 endpoints**.
- Wiring testé sur DB SQLite : Free Beta 120cr, etude_marche débite 40 + archive, dégradé=0+pas d'archive, admin=0.
- Mémoire testée : onboarding faux sans profil / vrai après company_name, injection formate les champs, champs inconnus ignorés.

## Non fait / différé
- **Parcours e2e réel** toujours non exécuté (Docker indispo → pas de Postgres/Qdrant, pas de clés valides). Tout est testé en unitaire/SQLite.
- Preview/paywall (pending_reports) : toujours pas repris (simplification assumée).
- Sujets ouverts inchangés (analytics hébergement, Supabase dev, migration Docker).

## Prochaines étapes
1. **P6 — Worker + PII** : worker planifié (veilles/agents cron + emails), sidecar Presidio (client PII + redaction + audit), durcissement sécurité.
2. **P7 — Migration & bascule** (données prod → nouveau schéma, users conservés).
3. Frontend Next.js (responsive) — pas encore commencé (P0 prévoyait le design system ; le front lui-même reste à bâtir/porter).

## Contexte à ne pas oublier
- Facturation : precheck (402 avant l'appel) → run → finalize (débit+archive+track), **jamais facturé si dégradé**. Admin bypass partout.
- Injection mémoire = build_context(db, user_id), préfixée aux prompts analysis + agents.
- Onboarding = profil avec company_name (gate côté front via /memory/onboarding-status).
- JSON cross-dialecte via JSONType ; datetimes normalisés UTC dans billing.
- Snapshots : 17-59, 18-10, 18-30, session-recap, 19-00 (P3), 19-20 (P4), 19-40 (P5).
