# Snapshot — 2026-08-12 (Wiring backend : Auth + Onboarding branchés)

## Contexte
Le prototype exact tourne en build Next compilé (racine localhost:3005). Phase de wiring :
remplacer les mock par les vraies API, écran par écran, dans `app/_prototype/App.jsx`.

## Fait : pont API + Auth + Onboarding (VÉRIFIÉS bout-en-bout dans l'UI exacte)
### Pont API
- `frontend/app/_prototype/bridge.js` : axFetch + token localStorage + axRegister/axLogin/axMe/
  axSaveProfile/axGetProfile/axBalance/axPlans. Base = NEXT_PUBLIC_API_URL (http://127.0.0.1:8090).
- Importé en haut de App.jsx.

### Auth (branché)
- AuthPage : `finish` rendu **async**, passe le **pwd**, gère erreurs (état `authErr` + affichage), Google → message "bientôt".
- Router App : `onSubmit` de <AuthPage> → **await axRegister/axLogin** puis go('onb1'|'app').
- VÉRIFIÉ : inscription `founder2@axialtest.com` via l'UI exacte → POST /auth/register 8090 → user créé dans Postgres (dev_users). Freemail rejeté 400 (preuve backend).

### Onboarding (branché)
- Backend : `memory.onboarding_complete` assoupli → complet dès qu'un champ contexte (sector/stage/challenge/market/company_name) est rempli (l'onboarding produit ne collecte pas de company_name).
- App.jsx : `onLaunch` de <OnbStep3> → **await axSaveProfile({sector, funding_stage:stage, main_challenge:challenge, target_market:geo})** puis go('app').
- VÉRIFIÉ : parcours pills (SaaS B2B/Seed/Comprendre marché/France) → profil réel en base company_profiles.

## Parcours validé à l'écran (UI = prototype exact)
Landing → Commencer → Auth (inscription réelle) → Onboarding 3 étapes exactes (contexte pills, aperçu analyse, "Lancer") → **entrée dans l'app shell exact** (sidebar Conversations/Rapports/Agents/Mémoire/Crédits/Docs/Paramètres).

## RESTE À BRANCHER (encore en mock dans l'app shell)
- **Crédits** top-bar (affiche 2481/5000 mock) + surface Crédits → /billing/balance + /plans.
- **Workspace / Conversations** (réponse "Bonne question..." = mock streaming) → /intelligence projects+conversations+messages.
- **Agents** → /intelligence/agents + /agents/route.
- **Rapports** → /analysis/run + /reports (+ PDF).
- **Mémoire** surface → GET/PUT /memory/profile (afficher le profil onboardé).
- Déconnexion → axClearToken.

## Méthode de wiring (rappel)
Éditer les "coutures" dans App.jsx : trouver le handler mock (ex. onSubmit, onLaunch, handleSendNew,
data initiale), remplacer par appel bridge. Capturer à chaque écran. `var {..}=React` déjà appliqué.

## Serveurs / lancement
- Backend 127.0.0.1:8090 (uvicorn --reload, auth locale, Postgres axial). Front localhost:3005 (racine=App).
- Docker occupe 8080+3000 (ancien insight-map) — ne pas toucher.
- Comptes test : founder2@axialtest.com / password123 (onboardé). mira@, founder@, jean@.

## Snapshots
…, 18-10 (conversion build Next), 18-40 (auth + onboarding branchés & vérifiés).
