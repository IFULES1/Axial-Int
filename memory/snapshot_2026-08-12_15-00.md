# Snapshot — 2026-08-12 (Backend validé en réel + prototype front reçu)

## Objectif
Faire tourner le backend en réel SANS Docker + réceptionner le prototype frontend.

## Backend live validé (ZÉRO Docker) — terminé
- **Postgres 16 installé via Homebrew** (`brew install postgresql@16`), service lancé
  (`brew services start postgresql@16`). Binaires : `/opt/homebrew/opt/postgresql@16/bin`.
- Base **`axial`** créée (`createdb axial`), user `mirad`.
- **DATABASE_URL** = `postgresql+psycopg://mirad@localhost:5432/axial`.
- **Qdrant en mode `:memory:`** — toggle ajouté dans `rag/vector_store.py` : URL vide ou `:memory:`
  → `QdrantClient(location=":memory:")`. AUCUN serveur Qdrant requis en dev.
- **5 migrations Alembic appliquées sur Postgres réel** (0001→0005). Pas juste SQLite.
- **Smoke-test `scripts/smoke.py`** OK sur Postgres réel : Free Beta 120cr, mémoire+onboarding+contexte,
  analyse facturée 40cr→solde 80, rapport archivé + PDF 1662o, Qdrant in-memory 2 passages, veille planifiée.
- Fix : `vector_store.search` migré `.search()` → **`.query_points()`** (API qdrant-client récente).
- Non-régression : **40 tests passent**, ruff clean.

## Comment relancer le backend en local (rappel)
```
export PATH="/opt/homebrew/opt/postgresql@16/bin:$PATH"
export DATABASE_URL="postgresql+psycopg://mirad@localhost:5432/axial"
export QDRANT_URL=":memory:"
export PYTHONPATH=.
cd ~/axial-intelligence
.venv/bin/alembic upgrade head      # migrations
.venv/bin/python scripts/smoke.py   # smoke
.venv/bin/uvicorn app.main:app --port 8080   # API
```
(TODO utile : écrire un `.env` local avec ces valeurs pour éviter les exports.)

## Prototype frontend reçu
- Fichier : `/Users/mirad/Downloads/Axial Intelligence (2).html` → copié dans
  **`~/axial-intelligence/frontend/prototype/axial-prototype.html`** (1.9 Mo, 203 lignes).
- Nature : **React mono-fichier, Babel in-browser (`text/babel`)**, **données MOCK** (pas d'API réelle, 1 seul fetch).
  S'ouvre directement dans un navigateur.
- ~55 composants couvrant TOUS les écrans spec : LandingPage, AuthPage, OnbShell (onboarding),
  AppShell, Workspace (Composer/ConvThread/ConversationsRegion), AgentsLibrary/AgentSession/AgentWizard,
  ReportsEditor/Generating/Quota, MemorySurface, CreditsSurface, SettingsSurface, Docs*, CitationPanel,
  ShareModal, SourceConflictModal, TweaksPanel (panneau de réglage design).
- **Alignement backend quasi 1:1** (voir table dans le message).

## Prochaines étapes (décision en attente)
Intégration frontend — options :
1. **Porter en Next.js** (cible du plan) : structure app/, routing, Supabase auth, remplacer mock→API réelles. Robuste, plus long.
2. **Garder mono-fichier + câbler** les mock aux vraies API. Rapide, non production-grade.
Puis : P7 migration/bascule en dernier.

## Sujets ouverts (inchangés)
- Hébergement analytics (analytics Supabase non créé — limite 2 projets).
- Supabase dev pour tester auth/register e2e (register nécessite Supabase live ; pas encore branché).
- Preview/paywall non repris.

## Contexte à ne pas oublier
- Backend P0→P6 COMPLET + câblé + **validé sur Postgres réel**. 40 tests, 8 tables, 5 migrations.
- Qdrant :memory: = dev only ; prod = vrai URL Qdrant.
- Register/login e2e nécessitent Supabase (URL+clés dans .env) — non testé (pas de Supabase dev branché).
- Snapshots précédents : …, 20-05 (câblage+mémoire), 20-30 (P6).
