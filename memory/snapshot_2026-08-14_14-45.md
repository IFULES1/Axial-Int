# Snapshot — 2026-08-14 (après-midi) — Qdrant serveur + chat e2e validé + rerank combiné

## Objectif de la session
Finir l'ingestion de la base de connaissance, sortir Qdrant du mode embedded, et
**valider le chat Workspace de bout en bout depuis l'UI** — puis améliorer la qualité RAG.

## Tâches complétées
- [✓] **Ingestion KB terminée** (clé Cohere payante). Batch test 15 docs ≈ 0,11 $ ; run complet.
  État final : **218 docs / 72 669 chunks**. ~31 docs restants ont échoué (disque plein en cours de run → `OperationalError: unable to open database file`, PAS Cohere).
- [✓] **Tri disque** → disque externe `IFULES-X1` monté sous `/Volumes/IFULES-X1/Axial-Backup/` :
  99 photos, 45 vidéos, Photo Booth, Screen Recordings, + **les 3 dossiers corpus originaux** (backup). **Mac : 5 → 14-17 Go libres.**
  Reliquats à faire par l'utilisateur : `sudo rm -rf "/Users/mirad/Movies/Screen Recordings"` (root-owned), `.dmg` + VSCode.app dans Downloads (optionnel).
- [✓] **Qdrant migré embedded → serveur natif** (binaire v1.19.0, PAS Docker) :
  - Binaire dans `bin/qdrant`, stockage `data/qdrant-server/`, **port 6355** (6333 = ancien Docker, ne pas toucher).
  - **72 669 vecteurs migrés SANS ré-embedding** (copie directe via `scripts/migrate_qdrant_to_server.py`, 0 $).
  - `QDRANT_URL` dans Doppler = `http://localhost:6355`. Relance : `./scripts/start_qdrant.sh`.
- [✓] **Chat Workspace validé E2E** (API **et** à l'écran dans le navigateur). Login founder2 → question → analyse PESTEL sourcée (web + interne).
- [✓] **3 bugs réels corrigés grâce au test à l'écran :**
  1. **Crash front** `content.map is not a function` (`App.jsx:1861`) — le proto attend un tableau, l'API renvoie une string → normalisation en tableau dans `AiMsg`.
  2. **`/health/providers` obsolète** (listait perplexity/openai comme requis) → réécrit sur le vrai stack (`app/shared/health.py`).
  3. **Pas de failover LLM** — un 503 Gemini dégradait la réponse → `llm_client.generate()` a maintenant une **chaîne de fallback runtime** (Gemini plante → Claude) + retry sur 5xx/429 transitoires dans `gemini.py`.
- [✓] **Sources internes remontées comme citations** (avant : la KB nourrissait le LLM mais n'était jamais citée). Taguées 📚 « interne ».
- [✓] **Chantier B — rerank combiné web + interne** : nouveau `_assemble_sources()` dans `intelligence/service.py` fusionne web + passages internes en UN pool reranké Cohere (`rerank_indices()` ajouté à `app/shared/search/rerank.py`) → contexte ET citations en ordre de pertinence unifié. Dédup web par titre+domaine.
- [✓] **Tests** : 2 tests obsolètes mis à jour (`test_health_providers_shape`, `test_degrades_when_web_search_unavailable` → `test_degrades_when_llm_unavailable`). **45/45 passent.**

## Décisions techniques prises
- **Qdrant = binaire natif local** (pas Docker : trop lourd, 19 Go ; pas Cloud : RGPD). 1 variable à changer, code déjà compatible mode serveur (`vector_store._client()` ligne 53 → `QdrantClient(url=)`).
- **Migration par copie de vecteurs** plutôt que ré-ingestion → économise ~2 $ + 15 min.
- **Failover LLM runtime** (pas seulement « si non configuré ») car les 503 Gemini sont fréquents et transitoires.
- **Rerank combiné** : web et interne ont des scores non comparables (rerank vs cosinus) → un seul rerank Cohere sur le pool fusionné les met à égalité.

## État courant du système
- **Qdrant** : serveur natif UP sur `http://localhost:6355`, collection `knowledge_base` = **72 669 points** (statut green). Ancien store embedded `data/qdrant/` conservé (backup, non utilisé).
- **Backend** : UP sur `127.0.0.1:8090`. Lancer : `doppler run -- .venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8090`.
- **Frontend** : UP sur `localhost:3005`. Lancer : `cd frontend && npm run dev -- -p 3005` (défaut API = 8090, pas besoin de doppler côté front). `.claude/launch.json` màj pour port 3005.
- **Disque** : ~14 Go libres (était 5). Externe IFULES-X1 branché avec les backups.
- **Compte test** : `founder2@axialtest.com` / `password123` (118 crédits).
- ⚠️ Docker occupe 6333 (Qdrant) + 3000 + 8080 (ancien insight-map) — NE PAS TOUCHER. Axial = 6355 + 8090 + 3005.

## Problèmes résolus
- Clé Cohere 401 après ajout carte (ancienne clé révoquée) → régénérée + `doppler secrets set COHERE_API_KEY`.
- Ingestion coupée à 218/249 docs = **disque plein** (pas Cohere). Réglé par le tri disque.
- Port 6333 pris par Docker → Qdrant natif sur 6355.
- Streaming client lent (typewriter) = détail de polish UX non bloquant (voir `App.jsx` `streamingSpeed`).

## Prochaines étapes (ordre de priorité)
1. **Finir les ~31 docs KB restants** : maintenant que Qdrant est un serveur (multi-process) + disque OK, relancer `scripts/ingest_knowledge_base.py` (il reprendra) — MAIS il écrit dans le serveur 6355 via QDRANT_URL, vérifier que l'ingestion pointe bien dessus. Gérer les PDF scannés (OCR plus tard).
2. **Brancher les 2 autres modes au front** : Strategic Agents (PESTEL/Porter, `analysis.run_analysis` déjà câblé, tier=report/Claude) + Reports. Tester rendus + conso crédits.
3. **Mémoire founder** : vérifier que le profil onboarding enrichit réellement les prompts (`memory.build_context`).
4. **Polish** : accélérer le streaming du chat (streamingSpeed), afficher les chips de citation internes 📚 distinctement des web 🌐 dans l'UI.
5. **Industrialisation (backlog `docs/PISTES_AMELIORATION.md`)** : bascule embeddings B→C (BGE-m3), Qdrant sur VPS pour la prod, durcissement VPS, Doppler prod, DOCX/OCR, capture dataset fine-tuning.

## Contexte à ne pas oublier
- **Doppler** = secrets, TOUT via `doppler run -- …`. `QDRANT_URL=http://localhost:6355`.
- **Qdrant serveur doit tourner** avant backend/ingestion : `./scripts/start_qdrant.sh` (relance après reboot).
- **Migration vecteurs** = `scripts/migrate_qdrant_to_server.py` (rejouable si besoin).
- Le code LLM/embeddings/recherche est provider-agnostique (config) → changer de modèle = 1 variable.
- **Failover LLM** : chat=Gemini→Claude, report=Claude→Gemini. `generation_available()` = au moins un des deux.
- Snapshots antérieurs : `snapshot_2026-08-13_reprise.md` (chat branché), `_19-00.md` (APIs+RAG+corpus), `_coeur.md` (le cœur marche).
