# Snapshot — 2026-08-10 (P4 Intelligence)

## Objectif
Implémenter P4 : couche intelligence (projets/conversations/messages) + agents
Market Scanner (PESTEL) & Competitor Radar (Porter) sur moteur générique, avec
non-chevauchement + bloc "AXIAL Recommande".

## Tâches complétées (P4)
- [✓] **`app/modules/intelligence/personas.py`** — moteur d'agents générique :
  - dataclass `AgentPersona` (key, name, framework, system_prompt, scope_keywords, redirect_to/hint).
  - 2 personas natifs : MARKET_SCANNER (PESTEL) + COMPETITOR_RADAR (Porter). Registre `_REGISTRY`.
  - `AXIAL_RECOMMENDE_INSTRUCTION` ajouté à chaque `full_system_prompt()`.
  - `route(query, requested)` : scoring keywords → non-chevauchement (garde l'agent demandé mais renvoie une note de redirection si l'autre score ≥2 et > demandé). DEFAULT_AGENT=market_scanner.
  - Conçu pour accueillir les agents custom (Startup Pro, 10 max) = mêmes AgentPersona construits depuis la DB plus tard.
- [✓] **`models.py`** : Project → Conversation → Message (UUID, cascades, JSONB citations).
- [✓] **`service.py`** : create/list projects, create/list conversations, list messages, **`post_message()`** (route → persona → RAG best-effort → web-search generate avec dégradation propre → persiste user+assistant, incrémente compteurs). Note de redirection préfixée à la réponse si non-chevauchement.
- [✓] **`router.py`** `/intelligence/*` : GET /agents (public), POST /agents/route, CRUD projects, conversations, messages (GET/POST). run/messages protégés.
- [✓] main.py monte le routeur ; env.py importe les modèles.
- [✓] **Migration `0002_intelligence.py`** (projects/conversations/messages + FK cascade + index). Chaîne : `0001_documents -> 0002_intelligence (head)`.
- [✓] Tests `tests/test_intelligence.py` : routing macro→market_scanner, concurrentiel→competitor_radar, **note de redirection non-chevauchement**, AXIAL Recommande présent, défaut sans signal, routes montées/protégées. **21 passed total**, ruff clean.

## État vérifiable
- `.venv/bin/pytest -q` → **21 passed**. ruff clean.
- `alembic history` → base→0001→0002 (head). 4 tables : documents, projects, conversations, messages.
- Endpoints intelligence montés : /intelligence/agents, /agents/route, /projects (GET+POST), /projects/{id}/conversations (GET+POST), /conversations/{id}/messages (GET+POST).

## Non fait / différé
- **post_message non testé e2e** : nécessite DB + PERPLEXITY_API_KEY (dégradation testée en unitaire via routing).
- **Injection Mémoire (profil entreprise)** : hook `TODO(memory)` dans service.post_message — la couche Mémoire (P dédiée) reste à faire.
- Routing = heuristique keywords (pas de classifieur LLM comme le legacy Haiku). Suffisant ; peut être renforcé.
- Agents custom (création par user) : moteur prêt, endpoints de création d'AgentInstance pas encore exposés.
- Sujets ouverts inchangés (analytics hébergement, Supabase dev, Docker/migration e2e).

## Prochaines étapes
1. **P5 — Billing + Reports** : crédits (trial/free/purchased) + coûts par type + consommation atomique + Stripe ; persistance rapports + export PDF (ReportLab). Migration 0003.
2. Couche Mémoire (profil entreprise + injection auto) — soit avant P5 soit intégrée à P5/P6.
3. P6 Worker+PII → P7 Migration.

## Contexte à ne pas oublier
- Agents = personas dans un registre ; natif et custom = même AgentPersona (moteur générique voulu par le plan / Startup Pro).
- Non-chevauchement = non destructif : on garde l'agent demandé, on suggère l'autre via redirect_note.
- "AXIAL Recommande" = bloc obligatoire fin de réponse (différenciateur produit), injecté via full_system_prompt().
- post_message persiste 2 messages (user+assistant) et incrémente message_count/last_message_at.
- Snapshots : 17-59, 18-10, 18-30, session-recap, 19-00 (P3).
