# Snapshot — 2026-08-12 (Frontend scaffold Next.js + fix disque)

## Objectif
Échafauder le frontend Next.js (cible du plan) + démarrer le portage du prototype.

## Fix disque (bloquant résolu)
- Disque local était **plein à 100 % (153 Mo libres)** → écritures échouaient.
- Cause : image disque **Docker Desktop = 26 Go** (`~/Library/Containers/com.docker.docker/Data/vms/0/data/Docker.raw`), inutilisée (on est Docker-free).
- Action (avec feu vert explicite user) : quit Docker + `rm -rf ~/Library/Containers/com.docker.docker` → **26 Go récupérés, 30 Go libres maintenant**. (Reste un dossier métadonnées protégé macOS, insignifiant.)
- Downloads 5.8G, Movies 3.1G, Notion 8.7G, app Claude 8.5G restent si besoin de plus.

## Frontend scaffold (fait) — `~/axial-intelligence/frontend/`
- Stack : **Next.js 14.2 (App Router) + React 18 + TS + Tailwind 3.4 + @supabase/supabase-js**. Node 26/npm 11.
- Config : package.json, tsconfig, next.config.mjs, postcss, **tailwind.config.ts** (palette violette portée du prototype), .gitignore, .env.local.example.
- **Design tokens** (`app/globals.css`) portés du prototype : dark-first (bg #07050F, surface #110D1E, violet #7976F7, fg #F0F0F5) + override light ([data-theme=light]), radii (input/button 10, card 14, modal 16, pill), police système + JetBrains Mono.
- `app/layout.tsx` + **`app/page.tsx`** (landing complète : hero dégradé violet, CTA, 3 cartes Workspace/Agents/Rapports+Mémoire).
- **`lib/api.ts`** : client API typé (apiFetch + api.get/post/put/del, ApiError, base NEXT_PUBLIC_API_URL=http://localhost:8080, header Bearer).
- **`lib/supabase.ts`** : client Supabase browser (null si non configuré → `supabaseConfigured` flag pour fallback mock).
- `.env.local` créé (API_URL=8080, Supabase vides pour l'instant).
- `npm install` OK (node_modules 269M).

## Vérifié (browser pane)
- `npm run dev` compile sans erreur. **Landing rendue OK sur desktop ET mobile (375px)** — screenshots pris.
- ⚠️ Port 3000 déjà occupé par une AUTRE app node ("Axial Intelligence - Strategic AI Platform", ancien front à l'user) → **mon dev tourne sur port 3005**.
- ⚠️ `preview_start` (outil) auto-détecte le backend Python à la racine et tente uvicorn → échoue. Contournement : `cd frontend && npm run dev -- -p 3005` en Bash, puis preview_start avec url http://localhost:3005.

## Non fait / prochaines étapes (portage écran par écran)
Ordre de portage prévu (réutiliser le design du prototype `frontend/prototype/axial-prototype.html`) :
1. **AuthPage** (/auth) → register/login. BLOQUÉ sur Supabase dev (voir sujets ouverts).
2. **Onboarding** (OnbShell) → PUT /memory/profile + gate /memory/onboarding-status.
3. **AppShell** (nav 5 écrans) + **Workspace** → /intelligence conversations/messages.
4. **Agents** (AgentsLibrary/Session/Wizard) → /intelligence/agents + /agents/route.
5. **Rapports** (ReportsEditor/Generating) → /analysis/run+stream, /reports (+ PDF).
6. **Mémoire** (MemorySurface) → /memory/profile.
7. **Crédits** (CreditsSurface) → /billing/balance + /plans.

## Sujets ouverts (inchangés, décision user en attente)
- **Supabase dev dédié** : user a choisi de le créer, MAIS bloqué par limite 2 projets actifs (pause un projet OU upgrade Pro). Nécessaire pour tester auth register/login e2e. Décision user à trancher (quel projet pauser / upgrade).
- Hébergement analytics : idem limite projets.

## Contexte à ne pas oublier
- Backend : `export DATABASE_URL=postgresql+psycopg://mirad@localhost:5432/axial QDRANT_URL=:memory: PYTHONPATH=. ; .venv/bin/uvicorn app.main:app --port 8080`. Postgres via `brew services` (postgresql@16).
- Frontend : `cd frontend && npm run dev -- -p 3005` (3000 occupé par autre app). API pointée sur 8080.
- Backend complet+validé (40 tests, 5 migrations sur Postgres réel, smoke OK).
- Snapshots : …, 20-30 (P6), 15-00 (backend live + proto reçu).
