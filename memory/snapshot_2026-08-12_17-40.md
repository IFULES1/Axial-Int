# Snapshot — 2026-08-12 (Rebase sur le prototype exact)

## Décision utilisateur
"Repars de 0 en utilisant mon prototype comme base et reproduis exactement." → Ma reconstruction
Tailwind (8 pages) est ABANDONNÉE comme base UI. On repart du design exact du prototype.

## Ce qui a été fait : extraction + exécution du prototype exact
Le fichier `frontend/prototype/axial-prototype.html` (1.9M) est un **bundle** (format __bundler) :
- `__bundler/manifest` (JSON) = 14 fichiers : React (00), ReactDOM (01), Babel (02), + 11 fonts woff2 (JetBrains Mono), tous base64+gzip.
- `__bundler/template` (JSON string) = **le vrai document HTML source** de l'app (496K).

Extraction (script python inline) :
- Décodé le template → `frontend/prototype/axial-source.html` (ton code source lisible) :
  **56 composants**, **3 blocs <style> = 82K de CSS**, **35 variables CSS**, données 100% MOCK
  (`data.js` MOCK_USER, `surfaces-data.js`, `theme.js` light/dark, `i18n.js` FR/EN), 50 useState, 0 fetch, 4 localStorage.
- Décodé libs + fonts (binaire) → `frontend/public/proto/{libs/react.js,react-dom.js,babel.js, fonts/*.woff2}`.
- Réassemblé un **index.html exécutable** (UUID → chemins locaux) → `frontend/public/proto/index.html`.
  Servi par Next à **http://localhost:3005/proto/index.html**.

## VALIDÉ À L'ÉCRAN — c'est EXACTEMENT le prototype
- Landing exacte : nav (Produit / Comment ça marche / Sources / Tarifs), toggle **FR/EN**, typo JetBrains Mono,
  hero "Le copilote stratégique **des décisions de fondateur.**", badge "L'INTELLIGENCE STRATÉGIQUE SUR DEMANDE".
- SPA interactive : clic "Commencer" → écran Auth exact ("Créez votre espace stratégique", **Continuer avec Google**,
  "OU PAR EMAIL", conditions). Bien plus riche que ma reconstruction.

## Composants du prototype (56) — la vraie liste
LandingPage, AuthPage, OnbShell (OnbStep1/2/3), AppShell, Workspace(Composer, ConvThread, ConversationsRegion,
ConvListPanel, EmptyConvState, UserMsg, AiMsg, CitationPanel), AgentsLibrary/AgentSession/AgentWizard,
ReportsEditor/Empty/Generating/Quota, MemorySurface, CreditsSurface, SettingsSurface,
DocsSurface(DocsAgents/Credits/Memory/Prompts/UseCases), ShareModal, SourceConflictModal, RecipientView,
TopControls, TweaksPanel(+Tweak* 12 sous-composants de réglage design), ChartBars, ChipsRow, DemoCard,
FeatureCard, Field, Icon, Lockup, LandingLangPill.

## Prochaine étape : convertir en vrai build Next + brancher le backend
Approche : porter le design EXACT (CSS 82K verbatim + composants) dans un build Next.js compilé
(pas Babel-in-browser), puis remplacer les mock (data.js/surfaces-data.js) par les appels API réels
(auth locale /auth, /intelligence, /analysis, /memory, /billing — déjà prêts et testés).
Écran par écran, capture à chaque étape pour garder la fidélité.

## État serveurs
- Backend : 127.0.0.1:8090 (auth locale, Postgres réel). Front Next : localhost:3005.
- Prototype exact servi : http://localhost:3005/proto/index.html (Babel-in-browser, mock data).
- ⚠️ Docker occupe 8080+3000 (ancien insight-map). Ne pas y toucher.

## À faire / ouvert
- Convertir prototype → Next build + wiring (gros chantier, écran par écran).
- Mes anciennes pages Tailwind (app/(app)/*, /auth, /onboarding) : à remplacer par le design du prototype (garder la logique API).
- Clés LLM (PERPLEXITY/OPENAI) pour génération réelle : toujours à ajouter au .env.
- Fichiers de travail : frontend/prototype/axial-source.html (source lisible), frontend/prototype/src/ + app_src/ (extractions).

## Snapshots
…, 17-00 (front reconstruit Tailwind — abandonné comme base), 17-40 (rebase prototype exact).
